{
  "name": "web-voice-recognizer",
  "version": "1.0.0",
  "description": "Web voice recognition sample by Web Speech API",
  "scripts": {
    "rmdst": "if [ -e dist ]; then rm -r dist; fi",
    "start": "npm run rmdst && parcel src/index.html --open --no-source-maps",
    "build": "npm run rmdst && parcel build src/index.html --public-url ./ --no-optimize --no-source-maps",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/komura-c/web-voice-recognizer.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/komura-c/web-voice-recognizer/issues"
  },
  "homepage": "https://github.com/komura-c/web-voice-recognizer#readme",
  "devDependencies": {
    "parcel": "^2.0.1"
  }
}
